{
  "name": "grand-tournament",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html app.js styles.css public dist/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^1.0.0",
    "happy-dom": "^12.10.3",
    "vitest": "^1.0.0"
  }
}
